
enum AppStatus{
  warning,
  success,
  error,
  loading,
  infos,
  unknown,
}

enum UserTypes{
  client,
  employee
}

enum OrderStatus{
  active,
  finished,
  pending
}

enum InterestsStatus{
  accepted,
  refused,
  pending
}

enum PricingTypes{
  hourly,
  total
}


