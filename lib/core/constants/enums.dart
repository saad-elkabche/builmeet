
enum AppStatus{
  warning,
  success,
  error,
  loading,
  infos,
  unknown,
}

enum UserTypes{
  client,
  employee
}

enum OrderStatus{
  active,
  finished,
  pending,
  stopped
}

enum InterestsStatus{
  accepted,
  refused,
  pending,
  taken
}

enum PricingTypes{
  hourly,
  total
}


enum Langs{
  frensh,
  english
}


